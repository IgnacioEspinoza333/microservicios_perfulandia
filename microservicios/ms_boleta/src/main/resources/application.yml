spring:
  application:
    name: ms_boleta

server:
  port: 8088

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
      
#Para evitar problemas de hostname
  instance:
    prefer-ip-address: true