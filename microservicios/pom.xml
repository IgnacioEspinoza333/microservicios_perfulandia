<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- Identidad del proyecto -->
    <groupId>com.example</groupId>
    <artifactId>microservicios</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>microservicios</name>
    <description>Proyecto multi-módulo de microservicios</description>

    <!-- Módulos -->
    <modules>
        <module>eureka-server</module>
        <module>ms_usuario</module>
        <module>ms_clientes</module>
        <module>ms_pedidos</module>
        <module>ms_producto</module>
        <module>ms_inventario</module>
        <module>ms_envio</module>
        <module>ms_pago</module>
        <module>ms_categoria</module>
        <module>ms_proveedores</module>
        <module>ms_boleta</module>
    </modules>

    <!-- Propiedades globales -->
    <properties>
        <java.version>21</java.version>
        <spring.boot.version>4.0.7</spring.boot.version>
        <spring.cloud.version>2025.1.1</spring.cloud.version>
    </properties>

    <!-- Gestión de dependencias -->
    <dependencyManagement>
        <dependencies>

            <!-- Spring Boot -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring.cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <!-- Plugins -->
    <build>
        <pluginManagement>
            <plugins>

                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                    </configuration>
                </plugin>

            </plugins>
        </pluginManagement>
    </build>

</project>