spring:
  application:
    name: ms_envio

server:
  port: 8084

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
      
#Para evitar problemas de hostname
  instance:
    prefer-ip-address: true